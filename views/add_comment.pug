extends layouts/layout

block content 
  h1(style="text-align: center; margin-top: 20px;") Nuevo Comentario

  form(action="/comments/create", method="POST" style="max-width: 400px; margin: 0 auto;")
    
    //- Id del post donde se agregara el comentario
    input(type="hidden", name="_id", id="_id", class="form-control" value=`${post._id}`)

    .form-group
      label(for="autor") Autor:
      input(type="text", name="autor", id="autor", class="form-control")

    .form-group
      label(for="mensaje") Mensaje:
      textarea(name="mensaje", id="mensaje", rows="4", class="form-control")

    .form-group
      label(for="fecha") Fecha:
      input(type="date", name="fecha", id="fecha", class="form-control")

    .form-group(style="text-align: center; margin-top: 20px;")
      button(type="submit", class="btn btn-primary") Agregar Comentario